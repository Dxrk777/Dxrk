name: Dxrk Agent Verification

on:
  workflow_call:
    inputs:
      agent:
        description: 'AI Agent to run (e.g. claude-code, opencode)'
        required: false
        type: string
        default: 'opencode'
      preset:
        description: 'Configuration preset'
        required: false
        type: string
        default: 'full-dxrk'

permissions:
  contents: read

jobs:
  dxrk-agent:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install uv
        uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        with:
          enable-cache: true

      - name: Install Dxrk
        run: uv sync --all-extras

      - name: Run Agent Setup & Verification
        run: |
          uv run dxrk-py --help
          uv run dxrk-py install --agent ${{ inputs.agent }} --preset ${{ inputs.preset }} --dry-run